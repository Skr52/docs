### Git 常用命令

``` js

1. git add  // 将代码存储到暂存区
2. git commit -m 描述  // 将存储区的代码添加并描述作用
3. git push origin master // 将暂存区的代码推送到远程主机分支上
4. git pull git地址  // 拉取最新的 git 仓库代码
5. git clone git地址 // 克隆最新的 git 仓库代码
// 后续完善。。。

```